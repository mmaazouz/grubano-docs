---
title: "API — Restaurants"
description: "Reference for /api/restaurants endpoints"
sourceHash: 7022ad66c991530b
---

{/* AUTO-GENERATED by scripts/generate-from-source.js — do not edit by hand. */}

# API — Restaurants

12 endpoint(s) under `/api/restaurants`.

## `GET · POST` `/api/restaurants`

Auto-detected endpoint at `/api/restaurants`. Description not yet documented.

**Auth:** Session required

**Query parameters**

| Parameter | Source |
|---|---|
| `city` | query string |
| `cuisine` | query string |
| `category` | query string |
| `q` | query string |
| `sort` | query string |
| `take` | query string |
| `skip` | query string |
| `lat` | query string |
| `lng` | query string |
| `limit` | query string |

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `201` | Created |
| `400` | Bad request |
| `401` | Authentication required |
| `403` | Forbidden |
| `409` | Conflict |
| `500` | Server error |

## `GET · PATCH · DELETE` `/api/restaurants/{id}`

Auto-detected endpoint at `/api/restaurants/{id}`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `401` | Authentication required |
| `403` | Forbidden |
| `404` | Not found |
| `500` | Server error |

## `GET · POST` `/api/restaurants/{id}/closures`

Auto-detected endpoint at `/api/restaurants/{id}/closures`. Description not yet documented.

**Auth:** Public

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `201` | Created |
| `400` | Bad request |
| `500` | Server error |

## `DELETE` `/api/restaurants/{id}/closures/{closureId}`

Auto-detected endpoint at `/api/restaurants/{id}/closures/{closureId}`. Description not yet documented.

**Auth:** Public

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `404` | Not found |
| `500` | Server error |

## `GET · POST` `/api/restaurants/{id}/connect`

Persist a freshly-mapped status (+ stamp stripeOnboardedAt on the FIRST pass to 'active'). No-op when nothing changed.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `403` | Forbidden |
| `404` | Not found |
| `500` | Server error |
| `502` |  |

## `GET` `/api/restaurants/{id}/finance/operations`

Auto-detected endpoint at `/api/restaurants/{id}/finance/operations`. Description not yet documented.

**Auth:** Session required

**Query parameters**

| Parameter | Source |
|---|---|
| `to` | query string |
| `from` | query string |
| `page` | query string |

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `401` | Authentication required |
| `403` | Forbidden |
| `404` | Not found |
| `500` | Server error |

## `GET` `/api/restaurants/{id}/finance/summary`

Auto-detected endpoint at `/api/restaurants/{id}/finance/summary`. Description not yet documented.

**Auth:** Session required

**Query parameters**

| Parameter | Source |
|---|---|
| `period` | query string |

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `403` | Forbidden |
| `404` | Not found |
| `500` | Server error |

## `GET · POST` `/api/restaurants/{id}/fulfillment`

Auto-detected endpoint at `/api/restaurants/{id}/fulfillment`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `401` | Authentication required |
| `403` | Forbidden |
| `404` | Not found |
| `500` | Server error |

## `GET · PUT` `/api/restaurants/{id}/hours`

Auto-detected endpoint at `/api/restaurants/{id}/hours`. Description not yet documented.

**Auth:** Public

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `500` | Server error |

## `GET` `/api/restaurants/{id}/invoices`

Auto-detected endpoint at `/api/restaurants/{id}/invoices`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `403` | Forbidden |
| `404` | Not found |
| `500` | Server error |

## `GET` `/api/restaurants/{id}/invoices/{invoiceId}`

Auto-detected endpoint at `/api/restaurants/{id}/invoices/{invoiceId}`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `403` | Forbidden |
| `404` | Not found |
| `500` | Server error |

## `PATCH` `/api/restaurants/{id}/pause`

Auto-detected endpoint at `/api/restaurants/{id}/pause`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `401` | Authentication required |
| `403` | Forbidden |
| `404` | Not found |
| `500` | Server error |

