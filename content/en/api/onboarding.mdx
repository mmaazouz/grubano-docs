---
title: "API — Onboarding"
description: "Reference for /api/onboarding endpoints"
sourceHash: e3873cab61d0496d
---

{/* AUTO-GENERATED by scripts/generate-from-source.js — do not edit by hand. */}

# API — Onboarding

3 endpoint(s) under `/api/onboarding`.

## `GET · POST` `/api/onboarding/chat`

DAC7 self-declaration complete enough to withdraw (read-only mirror of the affiliate withdraw rail's own check — replicated so that rail stays byte-identical; no money, no Stripe).

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `401` | Authentication required |
| `403` | Forbidden |
| `404` | Not found |
| `429` |  |
| `500` | Server error |
| `503` |  |

## `GET` `/api/onboarding/guide`

Auto-detected endpoint at `/api/onboarding/guide`. Description not yet documented.

**Auth:** Public

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |

## `GET` `/api/onboarding/unsubscribe`

Auto-detected endpoint at `/api/onboarding/unsubscribe`. Description not yet documented.

**Auth:** Public

**Query parameters**

| Parameter | Source |
|---|---|
| `token` | query string |

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |

