---
title: "API — Affiliate"
description: "Reference for /api/affiliate endpoints"
sourceHash: c34b64cc30a5f64a
---

{/* AUTO-GENERATED by scripts/generate-from-source.js — do not edit by hand. */}

# API — Affiliate

7 endpoint(s) under `/api/affiliate`.

## `POST` `/api/affiliate/apply`

Auto-detected endpoint at `/api/affiliate/apply`. Description not yet documented.

**Auth:** Public

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `404` | Not found |
| `429` |  |
| `500` | Server error |

## `GET · POST` `/api/affiliate/connect`

The SESSION operator (affiliate) + its affiliate Connect fields. Owner-scoped.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `404` | Not found |
| `500` | Server error |

## `POST` `/api/affiliate/content`

Auto-detected endpoint at `/api/affiliate/content`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `401` | Authentication required |
| `403` | Forbidden |
| `404` | Not found |
| `429` |  |
| `500` | Server error |
| `502` |  |

## `GET · POST` `/api/affiliate/join`

Auto-detected endpoint at `/api/affiliate/join`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `404` | Not found |
| `429` |  |
| `500` | Server error |

## `GET` `/api/affiliate/stats`

Auto-detected endpoint at `/api/affiliate/stats`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `404` | Not found |
| `500` | Server error |

## `GET · POST` `/api/affiliate/verify-request`

Auto-detected endpoint at `/api/affiliate/verify-request`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `401` | Authentication required |
| `404` | Not found |
| `409` | Conflict |
| `500` | Server error |

## `GET · POST` `/api/affiliate/withdraw`

SESSION operator (must be a real affiliate). Owner-scoped — never a client id.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `404` | Not found |
| `500` | Server error |
| `502` |  |

