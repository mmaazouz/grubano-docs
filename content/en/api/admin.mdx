---
title: "API — Admin"
description: "Reference for /api/admin endpoints"
sourceHash: 93cf56e96cf91c5a
---

{/* AUTO-GENERATED by scripts/generate-from-source.js — do not edit by hand. */}

# API — Admin

23 endpoint(s) under `/api/admin`.

## `GET` `/api/admin/claims`

Auto-detected endpoint at `/api/admin/claims`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `403` | Forbidden |

## `POST` `/api/admin/claims/{id}/arbitrate`

Auto-detected endpoint at `/api/admin/claims/{id}/arbitrate`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `401` | Authentication required |
| `403` | Forbidden |

## `POST` `/api/admin/claims/auto-approve`

Auto-detected endpoint at `/api/admin/claims/auto-approve`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `403` | Forbidden |
| `500` | Server error |

## `POST` `/api/admin/creator-earnings/mature`

Auto-detected endpoint at `/api/admin/creator-earnings/mature`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `403` | Forbidden |
| `500` | Server error |

## `POST` `/api/admin/creator-payouts/run`

Auto-detected endpoint at `/api/admin/creator-payouts/run`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `403` | Forbidden |
| `500` | Server error |

## `GET` `/api/admin/dac7/export`

Auto-detected endpoint at `/api/admin/dac7/export`. Description not yet documented.

**Auth:** Session required

**Query parameters**

| Parameter | Source |
|---|---|
| `year` | query string |
| `format` | query string |

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `403` | Forbidden |
| `500` | Server error |

## `POST` `/api/admin/franchise-settlements/run`

Auto-detected endpoint at `/api/admin/franchise-settlements/run`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `403` | Forbidden |
| `500` | Server error |

## `GET · POST` `/api/admin/influencer-verifications`

Auto-detected endpoint at `/api/admin/influencer-verifications`. Description not yet documented.

**Auth:** Session required

**Query parameters**

| Parameter | Source |
|---|---|
| `status` | query string |

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `401` | Authentication required |
| `403` | Forbidden |
| `404` | Not found |
| `409` | Conflict |
| `500` | Server error |

## `POST` `/api/admin/invoices/generate`

Auto-detected endpoint at `/api/admin/invoices/generate`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `401` | Authentication required |
| `403` | Forbidden |
| `500` | Server error |

## `GET` `/api/admin/ledger`

Auto-detected endpoint at `/api/admin/ledger`. Description not yet documented.

**Auth:** Public

**Query parameters**

| Parameter | Source |
|---|---|
| `days` | query string |

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `403` | Forbidden |

## `GET` `/api/admin/ledger/check`

Auto-detected endpoint at `/api/admin/ledger/check`. Description not yet documented.

**Auth:** Session required

**Query parameters**

| Parameter | Source |
|---|---|
| `to` | query string |
| `from` | query string |

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `401` | Authentication required |
| `403` | Forbidden |
| `500` | Server error |

## `GET` `/api/admin/llm-usage`

Auto-detected endpoint at `/api/admin/llm-usage`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `403` | Forbidden |
| `500` | Server error |

## `POST` `/api/admin/logistics/activation`

Auto-detected endpoint at `/api/admin/logistics/activation`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `401` | Authentication required |
| `403` | Forbidden |
| `404` | Not found |
| `409` | Conflict |
| `500` | Server error |

## `POST` `/api/admin/onboarding-nudges/run`

DAC7 self-declaration complete enough to withdraw (read-only mirror; no money, no Stripe).

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `403` | Forbidden |
| `500` | Server error |

## `GET` `/api/admin/operators`

Auto-detected endpoint at `/api/admin/operators`. Description not yet documented.

**Auth:** Public

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `403` | Forbidden |

## `GET` `/api/admin/overview`

Auto-detected endpoint at `/api/admin/overview`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `403` | Forbidden |

## `POST` `/api/admin/prestataires/coherence`

Auto-detected endpoint at `/api/admin/prestataires/coherence`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `401` | Authentication required |
| `403` | Forbidden |
| `404` | Not found |
| `500` | Server error |

## `GET` `/api/admin/reconcile-ghost-orders`

Auto-detected endpoint at `/api/admin/reconcile-ghost-orders`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `403` | Forbidden |
| `500` | Server error |

## `POST` `/api/admin/refunds/run`

Auto-detected endpoint at `/api/admin/refunds/run`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `401` | Authentication required |
| `403` | Forbidden |
| `500` | Server error |

## `GET` `/api/admin/restaurants`

Auto-detected endpoint at `/api/admin/restaurants`. Description not yet documented.

**Auth:** Public

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `403` | Forbidden |

## `POST` `/api/admin/restaurants/{id}/approve`

Auto-detected endpoint at `/api/admin/restaurants/{id}/approve`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `403` | Forbidden |
| `404` | Not found |
| `500` | Server error |

## `GET` `/api/admin/suppliers`

Auto-detected endpoint at `/api/admin/suppliers`. Description not yet documented.

**Auth:** Session required

**Query parameters**

| Parameter | Source |
|---|---|
| `status` | query string |

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `403` | Forbidden |

## `POST` `/api/admin/suppliers/coherence`

Auto-detected endpoint at `/api/admin/suppliers/coherence`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `401` | Authentication required |
| `403` | Forbidden |
| `404` | Not found |
| `500` | Server error |

