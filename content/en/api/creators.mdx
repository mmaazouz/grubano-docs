---
title: "API — Creators"
description: "Reference for /api/creators endpoints"
sourceHash: 6b70b5a2d1d1636b
---

{/* AUTO-GENERATED by scripts/generate-from-source.js — do not edit by hand. */}

# API — Creators

14 endpoint(s) under `/api/creators`.

## `POST` `/api/creators/apply`

Auto-detected endpoint at `/api/creators/apply`. Description not yet documented.

**Auth:** Public

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `201` | Created |
| `400` | Bad request |
| `500` | Server error |

## `POST` `/api/creators/apply/{id}/verify`

Coerce the stored Json dishConcepts into the strict shape vetCreator wants.

**Auth:** Public

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `404` | Not found |
| `500` | Server error |

## `GET · POST · PATCH` `/api/creators/campaigns`

Auto-detected endpoint at `/api/creators/campaigns`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `201` | Created |
| `400` | Bad request |
| `401` | Authentication required |
| `404` | Not found |
| `500` | Server error |

## `POST` `/api/creators/dishes`

Auto-detected endpoint at `/api/creators/dishes`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `201` | Created |
| `400` | Bad request |
| `401` | Authentication required |
| `403` | Forbidden |
| `500` | Server error |

## `PATCH · DELETE` `/api/creators/dishes/{id}`

Resolve the dish IFF it belongs to the session creator — else null (→404).

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `401` | Authentication required |
| `404` | Not found |
| `409` | Conflict |
| `500` | Server error |

## `POST` `/api/creators/dishes/{id}/restore`

Auto-detected endpoint at `/api/creators/dishes/{id}/restore`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `404` | Not found |
| `409` | Conflict |
| `500` | Server error |

## `POST` `/api/creators/dishes/{id}/submit`

Auto-detected endpoint at `/api/creators/dishes/{id}/submit`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `401` | Authentication required |
| `404` | Not found |
| `409` | Conflict |
| `500` | Server error |

## `POST` `/api/creators/dishes/photo`

Auto-detected endpoint at `/api/creators/dishes/photo`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `401` | Authentication required |
| `403` | Forbidden |
| `404` | Not found |
| `500` | Server error |

## `GET` `/api/creators/home`

Build a 30-day bar chart tracking recipe earnings (DishSale) and referral earnings (ReferralOrder) separately per day. `amount` = recipeAmount + referralAmount for backward compatibility.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `500` | Server error |

## `GET` `/api/creators/leaderboard`

Auto-detected endpoint at `/api/creators/leaderboard`. Description not yet documented.

**Auth:** Public

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |

## `PATCH` `/api/creators/me/roles`

Auto-detected endpoint at `/api/creators/me/roles`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `401` | Authentication required |
| `404` | Not found |
| `409` | Conflict |
| `500` | Server error |
| `503` |  |

## `GET` `/api/creators/my-dishes`

Auto-detected endpoint at `/api/creators/my-dishes`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `401` | Authentication required |
| `500` | Server error |

## `GET · PATCH` `/api/creators/profile`

Auto-detected endpoint at `/api/creators/profile`. Description not yet documented.

**Auth:** Session required

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `400` | Bad request |
| `401` | Authentication required |
| `403` | Forbidden |
| `500` | Server error |

## `GET` `/api/creators/public/{slug}`

Auto-detected endpoint at `/api/creators/public/{slug}`. Description not yet documented.

**Auth:** Public

**Responses**

| Code | Meaning |
|---|---|
| `200` | OK |
| `404` | Not found |

