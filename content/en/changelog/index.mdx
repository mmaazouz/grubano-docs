---
title: Changelog
description: "Grubano platform release history"
sourceHash: 68e8c2a0c4d05c2d
---

{/* AUTO-GENERATED by scripts/generate-from-source.js — do not edit by hand. */}

# Changelog

Auto-generated from commits on the main application repository.

## 2026-06-11

**✨ Features**

- **finance** — Finances section UI - the rail's storefront (A7)
- **i18n** — financeRail keys x5 locales (A7 dashboard finance)
- **finance** — A7 read endpoints - summary + paginated operations
- **emails** — transactional v1 - resa confirmation, owner/closure cancellation, refund confirmation (5-10j), no-show penalty notice (all post-success best-effort)
- **finance A6** — gardiens — finance unit tests + CI gate + cron probes + alerts
- **finance** — charge.refunded -> negative ledger lines + check extension (A5)
- **finance** — owner refund endpoints - bill + captured empreinte (A5)
- **finance** — refundPayment helper (pro-rata fee take-back, A5)
- **finance** — printable HTML invoice render (A4 V1)
- **finance** — monthly invoice generation + owner listing (A4)
- **finance** — Invoice model + gapless legal numbering (A4 migration)
- **connect-ui** — Encaissements card on establishment page (none/pending/active/restricted, Stripe-hosted onboarding CTA) + return toast on dashboard
- **finance** — golden-equation check endpoint + ledger backfill (A3)
- **finance** — webhook writes the ledger on every succeeded PI (A3)
- **finance** — LedgerEntry append-only model + write helper (A3)
- **finance** — route the empreinte hold to the resto's account (A2, fee 0)
- **finance** — route the bill charge to the resto's account (A2 dinein 5%)
- **finance** — lib/commission - single source of truth for rates (A2)

## 2026-06-10

**✨ Features**

- **finance** — webhook account.updated + dual signing secret (A1)
- **finance** — Express account creation + KYC onboarding endpoint (A1)
- **finance** — Restaurant Connect + commission fields (rail A1 migration)
- **finance** — generic PaymentIntent update/cancel helpers
- **hours-ui** — adoption card on dashboard, consumer badge+weekly hours+409 messages, order closed message with next opening, owner hoursWarning banner, completed reservation badge
- **hours-ui** — establishment Horaires section — weekly grid (multi-ranges, overnight, 24:00, apply-to-all, atomic PUT) + exceptional closures + conflicts modal (preview/confirm, empreintes released)
- **hours-ui** — i18n seed — hours namespace (grid/closures/conflicts/adoption), consumer badge+errors, completed badge, empreinte wording (no-show ou depart sans reglement)
- **hours** — expose hours on the public restaurant card (consumer badge data)
- **hours** — closure conflicts — preview, then confirmed mass-cancel + empreinte release
- **hours** — enforcement — availability, strict public booking, owner warning, order gate
- **hours** — central opening-hours engine + owner CRUD (weekly set + closures)
- **hours** — opening-hours data model — weekly ranges + closure exceptions (additive)
- **premium** — étape 3 commit 3 — clôture tracée (E/F) + notif & annulation (D) + historique résa (G)
- **premium** — étape 3 commit 2 — my-session endpoint (C) + client order (B)
- **premium** — étape 3 commit 1 — polling realtime (A) + empreinte cycle UI (H)
- **finance** — wire the empreinte hook on traced table closure
- **finance** — keep the empreinte active at arrival (premium-table cycle)

**🐛 Fixes**

- **finance** — webhook at-cent guard - never mark a ticket paid for less
- **finance** — /pay always charges the CURRENT amount due (stale-PI money bug)
- **hours-ui** — server-truth Ma session card (200+null purges stale pointer), ongoing sessions line in conflicts modal, hide legacy static hours when configured
- **hours** — a closure never cancels an ONGOING 'arrived' session
- **finance** — a completed reservation no longer blocks its table slot
- **finance** — terminate the reservation session on settlement (completed)
- **auth** — send the consumer welcome/confirmation email on registration

**📝 Documentation**

- **finance** — webhook is now the sole normal empreinte-release moment

## 2026-06-09

**✨ Features**

- **tickets** — per-reservation ticket history (GET /api/reservations/[id]/tickets)
- **tickets** — server soft-cancel a line + traced table closure (+ empreinte hook)
- **tickets** — client order-at-table endpoint (POST /api/t/[tableId]/order)
- **tickets** — premium-table data model — client/staff lines, options/notes, soft-cancel, closure trace
- **tables** — unpaid-previous alert + general "Clôturer la table" CTA
- **tickets** — owner-scoped purge of residual open tickets (cleanup)

