---
title: Changelog
description: "Grubano platform release history"
sourceHash: 1be61c95121d48d6
---

{/* AUTO-GENERATED by scripts/generate-from-source.js — do not edit by hand. */}

# Changelog

Auto-generated from commits on the main application repository.

## 2026-06-10

**✨ Features**

- **premium** — étape 3 commit 3 — clôture tracée (E/F) + notif & annulation (D) + historique résa (G)
- **premium** — étape 3 commit 2 — my-session endpoint (C) + client order (B)
- **premium** — étape 3 commit 1 — polling realtime (A) + empreinte cycle UI (H)
- **finance** — wire the empreinte hook on traced table closure
- **finance** — keep the empreinte active at arrival (premium-table cycle)

**🐛 Fixes**

- **auth** — send the consumer welcome/confirmation email on registration

**📝 Documentation**

- **finance** — webhook is now the sole normal empreinte-release moment

## 2026-06-09

**✨ Features**

- **tickets** — per-reservation ticket history (GET /api/reservations/[id]/tickets)
- **tickets** — server soft-cancel a line + traced table closure (+ empreinte hook)
- **tickets** — client order-at-table endpoint (POST /api/t/[tableId]/order)
- **tickets** — premium-table data model — client/staff lines, options/notes, soft-cancel, closure trace
- **tables** — unpaid-previous alert + general "Clôturer la table" CTA
- **tickets** — owner-scoped purge of residual open tickets (cleanup)
- **session** — brique A — #A3F2 short code + navigation list ↔ addition
- **tables** — TicketPanel — open per-service (arrived) or explicit walk-in
- **tickets** — lock the app/account pay channel to the linked client + arrived window
- **reservations** — record the consumer account on a public booking (userId)
- **schema** — Reservation.userId — link a reservation to a consumer account (additive)
- **eat** — "Payer mon addition" shortcut on /eat/account via localStorage handoff
- **bill** — /t/[tableId] QR landing — show bill + pay via Stripe Elements
- **finance** — webhook marks ticket paid + auto-releases the empreinte
- **finance** — bill payment endpoint (first real charge, capture automatic)
- **finance** — TableTicket bill-payment fields (Brique 2 migration)
- **tables** — minimal operator UI for the table addition (Addition tab)
- **tickets** — owner-scoped table-addition API (open / items / total)
- **schema** — TableTicket + TicketItem — table addition socle (Addition brique 1, additive)

**🐛 Fixes**

- **tickets** — bind ensureOpenTicket to the CURRENT reservation (no ghost items)
- **tickets** — session ticket — isolate the bill per reservation/service (confidentiality)
- **tickets** — open the bill payment to anyone; keep only the 'arrived' window

**📝 Documentation**

- **CLAUDE.md** — RÈGLE ABSOLUE — agents ne demandent JAMAIS d'autorisation

## 2026-06-08

**✨ Features**

- **tables** — single configurable "Acompte / empreinte" field wired to fulfillment
- **finance** — per-establishment deposit amount (no-show penalty = 100%)
- **finance** — one-shot owner endpoint to purge residual deposit holds
- **finance** — Stripe webhook for reliable deposit-status sync
- **tables** — operator deposit actions — release on arrival, capture on no-show
- **eat** — Stripe Elements card-hold on the consumer reservation flow
- **eat** — public reservation flow on /eat/r/[id]/reserver + auto-pick API
- **i18n** — add eat.reservation.* + tables.deposit.* keys for Stripe V1 UI
- **reservations** — public availability API (slots for the consumer booking flow)
- **reservations** — deposit empreinte endpoints (create hold / capture / release)
- **stripe** — server-only Stripe helper for deposit empreinte (manual capture)
- **schema** — Stripe deposit-hold fields on Reservation (additive, TEST mode)
- **tables** — printable A4 sheet for QR codes — window.print() + @media print
- **tables** — per-table QR codes in the Setup tab + PNG download
- **middleware** — make /t/[tableId] public (table-bill QR landing)
- **t** — public table-bill QR landing /t/[tableId] (sober shell, no payment yet)

**🐛 Fixes**

- **reservations** — refuse a reservation whose covers exceed the table capacity
- **deposit** — settle the empreinte on status transition so "Client arrivé" releases it
- **deposit** — release/capture read the LIVE PaymentIntent (Stripe = source of truth)

**🔧 Maintenance**

- **menu** — inline FR labels in CategoriesTab so they ship in the page bundle

