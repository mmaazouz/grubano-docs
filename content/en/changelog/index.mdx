---
title: Changelog
description: "Grubano platform release history"
sourceHash: 7b577cb3a55a2216
---

{/* AUTO-GENERATED by scripts/generate-from-source.js — do not edit by hand. */}

# Changelog

Auto-generated from commits on the main application repository.

## 2026-06-16

**✨ Features**

- **supplier-ux** — polish onboarding journey — register issues/SIREN, magic login (i18n+états+a11y), catalogue empty state
- **supplier-ux** — i18n pass — magic ns + supplier copy (no 48h, vous, SIREN, empty-state, CTAs) x5

## 2026-06-15

**✨ Features**

- **business-verification** — wire register to verifyBusiness + SIREN/SIRET field + instant success copy (no 48h)
- **business-verification** — registry-backed identity verification module + 'business_verification' gateway task
- **business-verification** — additive SupplierProfile fields (siren, officialName, verificationStatus, verifiedAt)
- **llm-quota** — admin GET /api/admin/llm-usage — per-partner usage + remaining (read-only)
- **llm-quota** — thread operatorId + graceful 429 degradation in authenticated LLM routes
- **llm-quota** — per-partner LLM quotas + gateway enforcement (graceful, FAIL-OPEN)
- **llm-gateway** — central LLM gateway — task->model map, token caps, metering, kill-switch
- **llm-gateway** — additive LlmUsage metering table
- **supplier-vetting** — i18n x5 (active/rejected success copy)
- **supplier-vetting** — outcome-aware register success (active/pending/rejected)
- **supplier-vetting** — admin status route (suspend/reject/activate) + block payment when not active
- **supplier-vetting** — auto-onboarding on register — vet -> active/pending/rejected (sync)
- **supplier-vetting** — LLM auto-onboarding vetting (legit/doubt/bad), injection-hardened, fail-safe
- **supplier-vetting** — additive vetting fields + 'rejected' status on SupplierProfile
- **supply-pay** — i18n x5 (pay action + paid badge + error)
- **supply-pay** — 'Payer' button + 'Payée' badge on the resto order history
- **supply-pay** — pay route (gated, ownership, confirmed, 5a amounts) + B2B payment webhook (idempotent) + history paymentStatus
- **supply-pay** — B2B payment lib — Stripe Checkout destination charge (amounts from 5a) + idempotent mark-paid
- **supply-pay** — additive payment fields on SupplyOrder (paymentStatus, checkout/PI ids, economics snapshot, paidAt)
- **supplier-connect** — i18n x5 (payout status + connect CTAs + gated message)
- **supplier-connect** — dashboard 'Paiements' card — status badge, onboarding CTA, gated message
- **supplier-connect** — onboarding API (gated, session-scoped) + SEPARATE B2B account.updated webhook
- **supplier-connect** — B2B Connect lib (gate + onboarding + status sync + webhook persist), reuses lib/stripe read-only
- **supplier-connect** — additive Connect fields on SupplierProfile (stripeAccountId, payoutStatus, onboardedAt)
- **supply-pricing** — pure B2B economics engine (take-rate + volume tiers + savings split, no money movement)
- **reorder** — i18n x5 (reorder panel + discovery search keys)
- **reorder** — 'À réapprovisionner' panel + discovery ?q search/filter + display link from /stocks
- **reorder** — read-only low-stock endpoint, operator-scoped (never writes stock)
- **reorder** — pure stock-status (replicates /stocks) + name-match + discovery search href
- **supply-order** — i18n x5 (status labels + supplier actions + resto history)
- **supply-order** — supplier status actions + badges, resto 'Mes commandes' history + cancel + nav

**🐛 Fixes**

- **auth** — multi-domain magic-link base via host allow-list (business vs app portal)
- **auth** — make the magic-link email reliably clickable

**🔧 Maintenance**

- **inbox** — rotate shared inbox v11 -> v12 (Agent 14)
- **inbox** — rotate shared inbox v10 -> v11 (Agent 14)

**♻️ Refactoring**

- **llm-gateway** — route API LLM calls through the gateway (behavior-preserving)
- **llm-gateway** — route lib LLM calls through the gateway (behavior-preserving)

**✅ Tests**

- **business-verification** — registry+LLM combine, FAIL-SAFE (review on incident/ambiguous), injection; re-point register-route test to verifyBusiness
- **llm-quota** — quota module, gateway enforcement, route 429 degradation + operatorId threading
- **llm-gateway** — gateway transport tests + re-point vetting tests at the gateway
- **auth** — magic-link multi-domain allow-list + exact-match host-injection guards
- **supplier-vetting** — mapping+fail-safe+injection, register flow, admin status, pay suspension gate
- **auth** — magic-link email — clickable button + raw-URL fallback + text part + canonical base
- **supply-pay** — amounts from 5a, gating/ownership/confirmed, idempotent mark-paid, pending-resume, webhook dispatch
- **supplier-connect** — gate default-closed, account-scoped webhook persist, status sync, onboarding scoping, separate webhook dispatch
- **supply-pricing** — exhaustive engine (flat, each tier+split, anti-double-charge, conservation sweep, rounding, edges, config override)
- **reorder** — stock-status/match helpers + read-only low-stock route (operator-scoped, low-only, no writes)
- **supply-order** — state machine valid/invalid transitions, ownership/isolation, cancel-only-before-confirm, history scoping

