---
title: Changelog
description: "Grubano platform release history"
sourceHash: 610c887084a23873
---

{/* AUTO-GENERATED by scripts/generate-from-source.js — do not edit by hand. */}

# Changelog

Auto-generated from commits on the main application repository.

## 2026-06-08

**✨ Features**

- **tables** — printable A4 sheet for QR codes — window.print() + @media print
- **tables** — per-table QR codes in the Setup tab + PNG download
- **middleware** — make /t/[tableId] public (table-bill QR landing)
- **t** — public table-bill QR landing /t/[tableId] (sober shell, no payment yet)
- **menu** — Categories tab — manage custom categories (CRUD)
- **menu** — owner-scoped CRUD for per-brand custom categories
- **schema** — per-brand custom menu categories (additive)
- **menu** — Manuel upload — file picker in DishEditor (new + edit photo)
- **menu** — soft photo warnings — non-blocking banner above the dish list
- **menu** — display real dish photo in the operator menu list
- **menu** — persist moderated dish photo into MenuItem.photos
- **menu** — Claude vision moderation before storing a dish photo (blocking)
- **menu** — owner-scoped dish-photo upload via server-signed Cloudinary
- **hub** — Danger Zone — delete this establishment (type-the-name confirm)
- **i18n** — add dashboard.hub.danger.* keys for delete-establishment UI

**🐛 Fixes**

- **menu** — "catégorie collée" — 4 defaults always available + fixed default
- **menu** — trim stored category + confirm no server remanence (sticky-category bug)
- **menu** — Scan IA bottom bar — no longer overflows the modal width
- **menu** — AI-scan flow sends its photo at creation so it persists end-to-end
- **menu** — persist dish photo atomically at creation (the real fix)

**🔧 Maintenance**

- **menu** — inline FR labels in CategoriesTab so they ship in the page bundle

**♻️ Refactoring**

- **photo** — shared dish-photo chain — moderation+warnings, square crop, no swallow

## 2026-06-07

**✨ Features**

- **establishments** — exclude archived establishments from all reads
- **restaurants** — DELETE establishment — smart soft/hard delete, owner-scoped
- **schema** — soft-delete establishments via Restaurant.archivedAt (additive)
- **tables** — reservation modal — block past date/time before submit
- **tables** — reservation modal — send durationMin + "libère à HH:MM" preview
- **tables** — Config — default reservation duration card
- **tables** — establishment switcher + restaurantId scoping + empty states
- **i18n** — add tables.* keys for switcher/duration/past-date
- **reservations** — configurable duration — establishment default + per-booking override
- **schema** — per-establishment default reservation duration (additive)
- **tables** — scope tables + reservations per establishment, owner-scoped
- **schema** — attach tables + reservations to an establishment (additive)
- **menu** — scope brand selector to current establishment + breadcrumb
- **hub** — 3rd access chip + migrate edit/delete brand actions on each card
- **i18n** — add hub brand-actions + brands page-create keys

**🐛 Fixes**

- **reservations** — reject a reservation whose slot is already in the past
- **menu** — manual dish creation writes to DB + owner-scope POST
- **menu** — freshness — no-store fetches + cross-tab refetch + dangling-brand recovery
- **brands** — attach new brand to the establishment it was created from
- **establishments** — mobile-readable card layout — no more truncated names

**🔧 Maintenance**

- **backfill** — table/reservation restaurantId backfill script (Chantier 2 step 2)

**♻️ Refactoring**

- **brands** — strip /brands to focused create-only page — no more list redundancy

## 2026-06-06

**✨ Features**

- **home** — consolidated 3-level accueil — ConsolidatedHome + thin page wrapper
- **i18n** — add dashboard.home.ov.* keys for consolidated 3-level home
- **dashboard** — add level-2 objectives to /api/dashboard/overview
- **dashboard** — consolidated home overview — multi-establishment aggregates + level-1 alerts
- **dashboard** — actionable "create brand" empty-states + clickable establishment cards (VAGUE 1 finitions)

**🐛 Fixes**

- **reservations** — stop 500 "Erreur serveur" on create when tableId is empty/invalid (VAGUE 1 bug 2)

