---
title: Changelog
description: "Grubano platform release history"
sourceHash: 891e8e1daff1ed8e
---

{/* AUTO-GENERATED by scripts/generate-from-source.js — do not edit by hand. */}

# Changelog

Auto-generated from commits on the main application repository.

## 2026-06-13

**✨ Features**

- **orders v2** — near-real-time polling + real chime + 3 tabs
- **orders v2** — factored order feed + light live endpoint

## 2026-06-12

**✨ Features**

- **eat** — order history on /eat/account - awaiting_payment / expired badges, no exclusion
- **orders** — customer awaiting-payment / expired tracking states + tests
- **creator M7.2** — purge des taux royalties en dur - les '4%' meurent, {pct} depuis AdoptionConfig
- **creator M7.1** — vetting durci v2 - verdict structure, perimetre elargi, multimodal photo, score signature
- **promos P2.2** — surfaces client - bandeau fiche, prix barres, ligne promo checkout, /eat/promos reel, home honnete
- **promos P2.1** — exposition lecture seule - resto promos additif, GET /api/eat/promos, recap discount
- **promos P1.3** — resto management UI + API + sidebar + i18n x5
- **promos P1.2** — server-side resolution at checkout + commission base
- **promos P1.1** — the pure promotion engine + schema trace
- **creator M6.2** — fiche recette UI - 4-section editor, allergen chips + suggestions, resto sheet screen, business case, public face
- **creator M6.1** — fiche recette de chef - schema + sheet logic + 3-faced API (D1 server lock)
- **creator M4.2** — Etoiles Grubano + marketplace UI - star scoring, badges, conditions from config
- **creator M4.1** — marketplace vivante - adoption email, waitlist promotion + accept/decline, adopters analytics
- **creator M3.2** — recipe editor UI - DishEditorModal, promotions page as single editing place, i18n x5
- **creator M3.1** — recipe editor API - draft/submit/vetting lifecycle, adoption lock (R1-R4), photo route
- **creator M2.3** — conditional studio - role-gated sidebar/home/revenus sections, dual labelled share kits (affiliation /ref + chef page), clean gates with one-tap re-enable, role toggles, real adoption rate everywhere (4% hardcode dead)
- **creator M2.2** — wizard role choice - two cumulable cards (at least one), forwarded to apply+verify; i18n x5 + parametric recipes badge
- **creator M2.1** — role split rails - isChef/isInfluencer flags (additive, default true), tolerant reader, home+earnings expose roles, verify upsert carries the wizard choice (never downgrades), self-service PATCH me/roles (at least one), additive influencer gate in /ref
- **tracking** — chef contribution rail - Order.chefSlug (additive), /api/chef-visit 24h last-touch cookie (never touches grubano_ref), bounded best-effort stamp at checkout, pageSales30d stat
- **chef** — public /chef/[slug] page - partners-first with opt-in proximity sort, social proof, portfolio, dual CTA; /chef + /ref made public in middleware; menu ?tab=adopt deep link
- **api** — public creator payload extended - servedAt lat/lng + social-proof volumes (sales/restaurants counts), additive
- **finance C3-fix** — order refunds - the A5xC1 gap closed

**🐛 Fixes**

- **orders** — pickup timeline = 4 steps, hand-off completes the order
- **orders** — ghost orders - card orders invisible until webhook-confirmed
- **checkout M7.3** — montant exact dans les phrases de paiement - fin de l'arrondi a l'euro
- **ui** — remove fake FRENCH10 promo (cart total now matches the real debit), promos showcase neutralized to sober coming-soon, /r/ workaround comment cleanup
- **creator** — earnings contract link is /ref/<slug> (the real route)

**📝 Documentation**

- **chef** — complement M1 - intent carved in code (photo fallback nominal, sales/restaurants divergence intended, city sort is the nominal path)

**🔧 Maintenance**

- **hygiene** — purge script DRY-RUN + royalty fallbacks aligned on the 0.02 doctrine
- **sync** — rotate inbox reports to Inbox v5
- **sync** — rotate inbox reports to Inbox v4
- **diag** — read-only creator-studio data inventory script (SELECT only, pivot prep)

**♻️ Refactoring**

- **eat/c** — permanent bridge to /chef - the affiliation rail is no longer reachable from any public creator page; internal links repointed

## 2026-06-11

**✨ Features**

- **creator B1** — earnings dashboard - lifecycle totals + 20EUR progress, copyable code/link + QR, paginated attributed orders with server badges, adopted-recipe earnings, payouts placeholder
- **creator B2a.3** — GET /api/creator/earnings (the B1 contract) + tests
- **creator B2a.2** — maturation engine - pure rules + endpoint + daily cron
- **creator B2a.1** — earnings lifecycle migration + B0-quater bonus param
- **emails v2** — account-email fallback + traced skips on cancellation emails, dedicated closure-cancellation email, full password reset flow (both spaces, single-use 1h token, security email)
- **checkout C2** — consumer payment journey - recap/Elements/confirmation page, order confirmation email + idempotent server confirm route, card orders routed through checkout
- **checkout C1.4** — B0 creator reconciliation in the referral flow
- **checkout C1.3** — webhook channel everywhere + order-paid branch
- **checkout C1.2** — POST /api/orders/[id]/pay - pickup/delivery charged
- **checkout C1.1** — channel + order payment + B0 config migration
- **resa** — consumer self-cancellation - window-gated route (releaseHold + cancelledBy + 2 emails), operator trace, Ma session cancel UI (additive migration: cancellationWindowHours, cancelledBy)

**🐛 Fixes**

- **ui** — shared referral-link builder replaces hardcoded grubano.com/ref links, delivery tracking gets the 5-step timeline (shared component, display only)
- **creator B1-bis** — referral link/QR rebuilt on /ref/<slug> with business-host neutralized, ghost 0% rates hidden on legacy adoptions, adoptions folded 5+5

**🔧 Maintenance**

- **sync** — rotate inbox reports to Inbox v3
- **sync** — route inbox reports to Inbox v2

