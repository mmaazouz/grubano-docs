---
title: Changelog
description: "Grubano platform release history"
sourceHash: 72d53e9b0e911bb7
---

{/* AUTO-GENERATED by scripts/generate-from-source.js — do not edit by hand. */}

# Changelog

Auto-generated from commits on the main application repository.

## 2026-06-06

**✨ Features**

- **home** — consolidated 3-level accueil — ConsolidatedHome + thin page wrapper
- **i18n** — add dashboard.home.ov.* keys for consolidated 3-level home
- **dashboard** — add level-2 objectives to /api/dashboard/overview
- **dashboard** — consolidated home overview — multi-establishment aggregates + level-1 alerts
- **dashboard** — actionable "create brand" empty-states + clickable establishment cards (VAGUE 1 finitions)
- **dashboard** — breadcrumb + establishment hub with clickable brands (commit C13-2)
- **brands** — copy a brand with its menu across establishments (commit 5C)
- **dashboard** — add-establishment flow + management page (commit 5B)
- **dashboard** — establishment selector + multi-establishment read (commit 5A)

**🐛 Fixes**

- **reservations** — stop 500 "Erreur serveur" on create when tableId is empty/invalid (VAGUE 1 bug 2)
- **establishments** — validate the address field (block "gogo", warn on geocode miss)
- **brands** — expose Brand.restaurantId on /api/brands/summary so the establishment hub can scope brands per restaurant (VAGUE 1 bug 1)
- **labels** — clarify "Actif"->"Sélectionné" + drop "dark kitchen" jargon
- **establishments** — validate the city field (block cuisine words, warn on geocode miss)
- **dashboard** — establishment switch no longer stuck spinning + clearer action

**♻️ Refactoring**

- **sidebar** — regroup nav by intention + anti-jargon labels (commit C13-1)

## 2026-06-05

**✨ Features**

- **schema** — add Brand.restaurantId + Restaurant.brands + openingHours (additive, Option B step 1)
- **business** — enrich establishment step in onboarding (cuisine, description, logo, cover)
- **business** — brand edit/delete/add from dashboard, owner-scoped routes
- **business** — resume onboarding at the right step (brand exists -> go to restaurant step)
- **account** — add suspended status + statusReason on Operator, login refused if suspended
- **business** — account-type selection screen routing to existing partner journeys (Approche A)
- **business** — partner onboarding (brand + restaurant + city), owner-scoped, restaurant inactive until review
- **business** — friendly verify-email confirmation page + partner page titles (i18n)

**🐛 Fixes**

- **sync** — notion-sync write inbox fail-loud + write-via-file + read-back confirmation
- **business** — partner host root routes to /business/start (account-type chooser) instead of /business/auth
- **business** — onboarding restaurant inactive by default + force onboarding when no brand + partner-aware dashboard identity
- **business** — host-based partner routing + public /business in middleware (Brique 2C fix)

**🔧 Maintenance**

- **data** — backfill Brand.restaurantId from operator's restaurant (Option B step 2)

**♻️ Refactoring**

- **schema** — Restaurant.operatorId no longer unique + Operator.restaurants[] plural (Option B step 4, cardinality switch)
- **reads** — use direct Brand.restaurantId link for menu + city resolution (Option B step 3, behavior-identical)

## 2026-06-04

**✨ Features**

- **business** — host-based routing + partner auth screen on business.grubano.com (i18n, design-system)
- **auth** — server-side role lock + secure partner registration (email verify, anti-bot, RGPD consent)
- **seo** — world-class metadata + dynamic OG images + hreflang + sitemap/robots + JSON-LD + PWA manifest
- **adoption** — waitlist join + city-taken visibility (L3-B)
- **ds** — DocsLink component (app→docs contextual help)
- **adoption** — city-exclusive recipes — block 2nd resto in city + waitlist model (L3-A)

**🔧 Maintenance**

- **seed** — 2nd test restaurateur in Orange (L3 testing fixture)

**✅ Tests**

- **qa** — vitest harness + risk-zone coverage + healthcheck + auto-alert to inbox/brain

**📦 Other**

- Revert "feat(seo): world-class metadata + dynamic OG images + hreflang + sitemap/robots + JSON-LD + PWA manifest"

## 2026-06-03

**✨ Features**

- **creators** — require 10k+ subscribers for verified badge (levier 2)
- **creators** — vet on real channel content (video titles + topics), conservative verdict
- **creators** — self-serve auto-verification UI on apply (brique C2)
- **creators** — auto-verify pipeline backend — bio-code ownership + youtube count + claude vetting (brique C1)
- **lib** — claude-based creator content vetting (json verdict)
- **lib** — youtube data api helper (subscriber count + channel info)

