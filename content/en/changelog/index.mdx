---
title: Changelog
description: "Grubano platform release history"
sourceHash: 3790c4db24b301e6
---

{/* AUTO-GENERATED by scripts/generate-from-source.js — do not edit by hand. */}

# Changelog

Auto-generated from commits on the main application repository.

## 2026-06-15

**✨ Features**

- **supply-pay** — i18n x5 (pay action + paid badge + error)
- **supply-pay** — 'Payer' button + 'Payée' badge on the resto order history
- **supply-pay** — pay route (gated, ownership, confirmed, 5a amounts) + B2B payment webhook (idempotent) + history paymentStatus
- **supply-pay** — B2B payment lib — Stripe Checkout destination charge (amounts from 5a) + idempotent mark-paid
- **supply-pay** — additive payment fields on SupplyOrder (paymentStatus, checkout/PI ids, economics snapshot, paidAt)
- **supplier-connect** — i18n x5 (payout status + connect CTAs + gated message)
- **supplier-connect** — dashboard 'Paiements' card — status badge, onboarding CTA, gated message
- **supplier-connect** — onboarding API (gated, session-scoped) + SEPARATE B2B account.updated webhook
- **supplier-connect** — B2B Connect lib (gate + onboarding + status sync + webhook persist), reuses lib/stripe read-only
- **supplier-connect** — additive Connect fields on SupplierProfile (stripeAccountId, payoutStatus, onboardedAt)
- **supply-pricing** — pure B2B economics engine (take-rate + volume tiers + savings split, no money movement)
- **reorder** — i18n x5 (reorder panel + discovery search keys)
- **reorder** — 'À réapprovisionner' panel + discovery ?q search/filter + display link from /stocks
- **reorder** — read-only low-stock endpoint, operator-scoped (never writes stock)
- **reorder** — pure stock-status (replicates /stocks) + name-match + discovery search href
- **supply-order** — i18n x5 (status labels + supplier actions + resto history)
- **supply-order** — supplier status actions + badges, resto 'Mes commandes' history + cancel + nav
- **supply-order** — status-transition APIs (supplier advance / resto cancel) + resto order history, ownership+transition gated
- **supply-order** — order state machine (placed→confirmed→preparing→delivered, cancel/decline) + shared operator resolver
- **supply-order** — i18n x5 (marketplace namespace + supplier reception keys) + seeder
- **supply-order** — resto discovery + per-supplier cart pages, supplier reception page + dashboard/marketplace entries
- **supply-order** — discovery + place-order + supplier-reception API (session-scoped, price snapshot, min-order enforced)
- **supply-order** — additive SupplyOrder + SupplyOrderLine (snapshots, cents) — distinct from private SupplierOrder & B2C Order
- **supplier-catalog** — i18n x5 (catalogue namespace) + idempotent seeder
- **supplier-catalog** — catalogue page + dashboard entry; generalize /supplier gate (all but landing/register)
- **supplier-catalog** — scoped CRUD + CSV import API (supplierProfileId from session, ownership-enforced)
- **supplier-catalog** — pure helpers (euros→cents, INCO, CSV parser) + session-scoped resolver
- **supplier-catalog** — additive SupplierCatalogItem model (FK to SupplierProfile, cents)

**🔧 Maintenance**

- **inbox** — rotate shared inbox v10 -> v11 (Agent 14)
- **inbox** — rotate shared inbox v9 -> v10 (Agent 14)

**✅ Tests**

- **supply-pay** — amounts from 5a, gating/ownership/confirmed, idempotent mark-paid, pending-resume, webhook dispatch
- **supplier-connect** — gate default-closed, account-scoped webhook persist, status sync, onboarding scoping, separate webhook dispatch
- **supply-pricing** — exhaustive engine (flat, each tier+split, anti-double-charge, conservation sweep, rounding, edges, config override)
- **reorder** — stock-status/match helpers + read-only low-stock route (operator-scoped, low-only, no writes)
- **supply-order** — state machine valid/invalid transitions, ownership/isolation, cancel-only-before-confirm, history scoping
- **supply-order** — snapshot/total, session-scoping, min-order guard, supplier isolation, route gates
- **supplier-catalog** — CSV parse, euros↔cents, cross-supplier ownership isolation, /supplier/catalog gate

## 2026-06-14

**✨ Features**

- **supplier** — i18n x5 (supplier namespace + roleSwitcher.spaceSupplier) + idempotent seeder
- **supplier** — supplier space shell + portal pages, role-aware nav + middleware gate
- **supplier** — passwordless provisioning bridge + self-serve registration/approval API
- **supplier** — additive SupplierProfile model + 'supplier' role value (Slice 0)
- **format** — single source of truth for money formatting (EUR, locale-aware)
- **nav** — multi-role space selector (RoleSwitcher) across the 3 shells (Phase 4)

**🐛 Fixes**

- **security** — gate operator flat routes in middleware (defence in depth)
- **format** — deterministic locale resolution in formatMoney (CI parity)

**🔧 Maintenance**

- **inbox** — rotate shared inbox v8 -> v9 (Agent 14)

**♻️ Refactoring**

- **money** — route remaining visible surfaces through format-money + surface tests
- **money** — design-system cards format via format-money (EUR, locale-aware)
- **money** — route consumer + creator money displays through format-money

**✅ Tests**

- **supplier** — provisioning no-clobber + role-spaces + middleware gate & singular/plural non-regression

